[project]
name = "ssamba"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.15.7",
    "wget>=3.2",
]

[tool.uv.extra-build-dependencies]
    causal-conv1d = ["torch"]
